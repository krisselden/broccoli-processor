{
  "name": "broccoli-processor",
  "version": "0.1.0",
  "description": "A higher level interface for proccessing input from broccoli that supports persistent caching, diffing input and one to many output.",
  "main": "dist/index.js",
  "typescript": {
    "definition": "dist/index.d.ts"
  },
  "typings": "dist/index.d.ts",
  "scripts": {
    "test": "mocha dist/tests",
    "prepublish": "rm -rf dist && tsc -p tsconfig.json",
    "watch": "rm -rf dist && tsc -p tsconfig.json --watch"
  },
  "author": "Kris Selden",
  "license": "MIT",
  "dependencies": {
    "broccoli-plugin": "^1.2.1",
    "fs-tree-diff": "^0.5.0",
    "mkdirp": "^0.5.1",
    "promise-map-series": "^0.2.2",
    "rsvp": "^3.2.1",
    "symlink-or-copy": "^1.1.3",
    "walk-sync": "^0.2.6"
  },
  "devDependencies": {
    "broccoli": "^0.16.9",
    "broccoli-test-helpers": "0.0.9",
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "tslint": "^3.11.0-dev.0",
    "typescript": "^1.9.0-dev.20160614-1.0"
  }
}
